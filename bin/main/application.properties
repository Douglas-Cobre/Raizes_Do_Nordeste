spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/raizes_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:8318}

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=false

app.security.jwt-secret=${JWT_SECRET:troque-esta-chave-por-uma-chave-segura-com-tamanho-suficiente}
app.bootstrap.admin-email=${BOOTSTRAP_ADMIN_EMAIL:admin@raizesdonordeste.com}
app.bootstrap.admin-password=${BOOTSTRAP_ADMIN_PASSWORD:Admin@123}
app.kpi.meta-pedidos-mensal=100
app.kpi.meta-faturamento-mensal=10000
